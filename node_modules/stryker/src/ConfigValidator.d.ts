import { TestFramework } from 'stryker-api/test_framework';
import { Config } from 'stryker-api/config';
export default class ConfigValidator {
    private strykerConfig;
    private testFramework;
    private isValid;
    private readonly log;
    constructor(strykerConfig: Config, testFramework: TestFramework | null);
    validate(): void;
    private validateTestFramework();
    private validateMutator();
    private validateThresholds();
    private validateThresholdValue(name, value);
    private validateThresholdsValueExists(name, value);
    private validateLogLevel();
    private validateTimeout();
    private validateCoverageAnalysis();
    private validateCoverageAnalysisWithRespectToTranspilers();
    private crashIfNeeded();
    private validateIsNumber(fieldName, value);
    private validateIsString(fieldName, value);
    private validateIsStringArray(fieldName, value);
    private invalidate(message);
    private joinQuotedList(arr);
}
