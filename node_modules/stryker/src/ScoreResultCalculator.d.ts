import { MutationScoreThresholds } from 'stryker-api/core';
import { MutantResult, ScoreResult } from 'stryker-api/report';
export default class ScoreResultCalculator {
    private readonly log;
    calculate(results: MutantResult[]): ScoreResult;
    determineExitCode(score: ScoreResult, thresholds: MutationScoreThresholds | undefined): void;
    private wrapIfSingleFileScoreResult(scoreResult);
    private calculateScoreResult(results, basePath);
    private copy(defaults, overrides);
    private determineFacts(basePath, results);
    private compareScoreResults(a, b);
    private calculateChildScores(results, parentName, basePath);
    private determineCommonBasePath(results, basePath);
    private countNumbers(mutantResults);
}
