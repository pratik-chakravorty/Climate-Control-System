import { File, InputFileDescriptor } from 'stryker-api/core';
import StrictReporter from './reporters/StrictReporter';
export default class InputFileResolver {
    private reporter;
    private readonly log;
    private inputFileResolver;
    private mutateResolver;
    constructor(mutate: string[], allFileExpressions: Array<InputFileDescriptor | string>, reporter: StrictReporter);
    resolve(): Promise<File[]>;
    private validateFileDescriptor(maybeInputFileDescriptors);
    private validateMutationArray(mutationArray);
    private markAdditionalFilesToMutate(allInputFiles, additionalMutateFiles);
    private logFilesToMutate(allInputFiles);
    private reportAllSourceFilesRead(allFiles);
    private reportSourceFilesRead(textFile);
    private filterTextFiles(files);
    private readFiles(inputFileDescriptors);
    private readInputFile(descriptor);
    private readLocalFile(descriptor, kind);
    private readInputFileContent(fileName, kind);
}
